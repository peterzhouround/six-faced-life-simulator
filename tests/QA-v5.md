# v5 验证记录 · 2026-09-01

## 自动规则测试

- `node --check game.js`、`node --check cloud.js`、`git diff --check`。
- `node tests/rules-v5.cjs`：v1—v4迁移、口粮移除、新手奖励/毕业礼防重复、6项支线、接取后计数、放弃重接、奖励装备比较、技能路线、战斗、旅行、主线门槛、存档验证、原型键拒绝、导入前恢复、存储失败、羁绊回忆不丢失、DOM ID、300回合模拟。
- 按天休息：100→40→0；不扣钱、不加经验、不增加主线计数；30天进位1个月；天数经过保存、读取、导出导入保留；旧档补0天。
- `node tests/cloud-v5.cjs`：模拟未配置/错误key、注册待验证、登录、退出、过期、401、网络失败、读写版本冲突；SQL静态检查。没有调用真实云账号或运行远程SQL。

## 浏览器交互

在本地测试网址进行，没有上传真实用户数据。

- 320×568：滚动至最后字段、修改愿望、确认开局；主按钮独占一行。无横向溢出。
- 360×640、390×844：重开确认、角色表单滚动并成功开始人生。
- 320×568：第一目标领奖→开启任务板→接取公会支线→两次工作→领取奖励；完成后按钮禁用。
- 320×568：手动存档；未配置时登录与上传禁用、提示真实状态；JSON文件导入预览、确认读取、恢复导入前人生。
- 320×568：导入满疲劳测试档，通过实际按钮连续休息两天，显示100→40→0、3岁→3岁2天，主线准备0/8。
- 1440×900：打开技能、修炼水球术、领取练习剑并显示已装备；打开完整地图。桌面无横向溢出。
- 页面错误日志检查未发现错误。临时视口覆盖已重置。

导出按钮生成JSON并显示成功提示；浏览器工具没有捕获到下载事件，因此没有据此宣称实际下载落盘通过。可恢复格式与导入路径已分别通过规则测试和浏览器文件选择测试。

窄视口检查不等于所有实体手机/Safari的验证。真实云登录、邮件投递、两账号权限隔离、跨设备冲突还需配置服务后验收，当前云配置保持为空。
