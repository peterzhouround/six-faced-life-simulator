# 无自建服务器的登录与云存档

游戏默认无需账号。本机自动存档、三个手动存档和 JSON 导入导出已经可用。云功能代码已接入，但配置为空，**尚未开通，不能跨设备自动同步**。

GitHub Pages 只托管静态网页；账号认证与用户存档需要另一个服务。本项目选择 Supabase 的托管 Auth + PostgreSQL，不需要租主机或自己运行服务器。托管服务的配额、邮件发送与费用由服务商决定，不承诺永久免费。不要为了开通功能直接购买付费方案。

## 站长配置步骤

1. 自行登录 Supabase，创建一个专用于本游戏的项目。不要把后台密码、数据库连接串或任何 secret / service_role key 放入 GitHub。
2. 在项目 SQL Editor 审阅并执行 `cloud/schema.sql`。它创建每用户一行的 `game_saves`，启用 RLS，只允许已登录用户访问自己的行；`save_game` 用版本号避免并发覆盖。
3. 在 Auth 中启用邮箱密码登录，保留邮箱验证。Site URL 设置为你的 GitHub Pages 游戏地址；验证邮件跳回后请再用邮箱密码登录。本版不接收 URL 中的登录凭证，不实现找回密码界面。
4. 面向公众开放注册之前配置并验证自己的 SMTP 邮件服务。默认邮件服务只适合测试，发送对象和频率有限制；不能因为测试邮箱收到信就认为所有玩家都可以注册。若项目启用了 CAPTCHA，本版还没有验证码界面，需先接入验证码组件，勿为绕过错误关闭安全保护。
5. 编辑 `cloud-config.js`：`url` 填 `https://你的项目ID.supabase.co`；`publishableKey` 填以 `sb_publishable_` 开头的公开客户端 key。这里故意不支持 JWT 形式的旧 anon key，避免误粘贴服务端高权限 key。
6. 提交并发布这一个公开配置文件。页面应从“尚未配置”变为“云服务已配置”。该提示只表示配置格式有效，不表示服务已通过验收。
7. 使用两个你自己控制的测试账号分别注册、验证、登录。A 上传后，在另一设备以 A 登录并读取；B 必须看不到 A 的人生。在 API 层额外验证 B 无法读写 A 的 user_id、未登录访问被拒绝；这是启用公网云存档前必须完成的权限验收。
8. 同账号在两设备分别打开上传确认。先确认一台，再确认另一台：后者应提示版本冲突。新云档同时创建时同样不应覆盖。

这些步骤需要你的项目与邮箱，当前版本只做了离线模拟请求测试和 SQL 静态检查，**没有实际部署数据库或完成真实账号/RLS 验收**。

## 玩家使用

- 不登录：直接游玩，每次行动自动保存在当前浏览器；在“存档”里导出 JSON 备份。无痕模式、清除网站数据、换浏览器、换网址都可能导致找不到旧档。
- 换设备：原设备导出 JSON，新设备导入并确认；TXT 传记不能恢复游戏。
- 配置云服务以后：在“存档 → 登录 / 云存档”注册或登录，手动“上传当前人生”；另一设备登录同账号，点击“读取云端人生”，核对后确认。
- 登录本身不会读取或覆盖本机；云端不会自动保存。每账号一个云档，本机三个手动档位不会上传。
- 导入或读取云端前会备份当前自动存档。通过“恢复导入前备份”返回；此备份只保留最近一次，重要人生仍应导出文件。
- 登录会话仅保留在页面内存，密码不保存。刷新页面、关闭页面或令牌过期后重新登录。退出登录不删除本机存档；在共用设备上不要留下私密的自由输入或角色信息。
- 网络超时时上传可能已经到达服务端。先读取检查，不要盲目反复覆盖。

## 安全边界

客户端 key 可以公开，但必须配合 RLS；秘密 key 永远不能进入前端。版本检查防止正常多设备误覆盖，不是反作弊系统。游戏允许本地编辑存档，不用于排名或交易。不要在自由行动中输入密码或个人敏感资料，它们会成为人生记录；主动上传时这些游戏记录也会发送到站长配置的 Supabase 项目。

## 官方资料

- [GitHub Pages 静态托管](https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages)
- [Supabase 邮箱密码登录与邮件发送](https://supabase.com/docs/guides/auth/passwords)
- [Supabase 自定义 SMTP](https://supabase.com/docs/guides/auth/auth-smtp)
- [行级安全 RLS](https://supabase.com/docs/guides/database/postgres/row-level-security)
- [保护 Data API](https://supabase.com/docs/guides/api/securing-your-api)
